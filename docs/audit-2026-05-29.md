# Полный аудит online-mafia — 2026-05-29

Многоагентный аудит: 11 finder-агентов (по подсистеме, 4 линзы — безопасность,
корректность, архитектура, тесты), каждая находка проверена двумя независимыми
adversarial-верификаторами (корректность чтения кода + реальный impact).

**Итог:** 80 сырых находок → 58 подтверждено (после верификации), 22 отклонено.
**Критичных дыр нет.** Auth/JWT, OAuth+PKCE, server-assigned LiveKit identity,
argon2id — реализованы корректно. Находки — это hardening, утечки ресурсов,
гонки в краевых случаях и техдолг (большие файлы).

Severity в таблицах ниже — после ручной перепроверки (workflow завышал часть
находок при эскалации; например path-traversal admin-gated → не high).

---

## ✅ Исправлено в этом коммите

### Группа A — безопасный hardening

| #   | Находка                                                     | Файл                       | Фикс                                       |
| --- | ----------------------------------------------------------- | -------------------------- | ------------------------------------------ |
| 8   | `loginInputSchema.password` без max-length → argon2 CPU-DoS | `shared/schemas/auth.ts`   | `.max(128)`                                |
| 11  | `joinLobby.password` без length-bound                       | `shared/schemas/lobby.ts`  | `.max(PASSWORD_MAX_LENGTH)`                |
| 38  | `emailSchema` без max-length                                | `shared/schemas/common.ts` | `.max(254)` (RFC 5321)                     |
| 9   | `ANTHROPIC_API_KEY` / `x-api-key` не в redact-paths         | `lib/logger.ts`            | добавлены пути                             |
| 12  | `clearSessionCookie` игнорировал `secure`/`sameSite`        | `auth.cookies.ts`          | передаём `baseCookieOptions()`             |
| 5   | Path traversal в debug-log (admin-gated)                    | `game/game.routes.ts`      | regex-guard `^[A-Za-z0-9_-]+$` до `join()` |
| 43  | Мёртвый код `void LOBBY;`                                   | `lobby/lobby.bots.ts`      | удалён + import                            |

### Группа B — frontend robustness

| #        | Находка                                                  | Файл                                                       | Фикс                                                                          |
| -------- | -------------------------------------------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 10,13,14 | Unhandled rejection в `emitGameAction`                   | `game.socket.ts`, `JudgePanel.tsx`, `SelfMediaButtons.tsx` | новый `fireGameAction()` (лог+swallow), все fire-and-forget вызовы переведены |
| 36       | `JSON.parse` без try/catch                               | `lib/api-client.ts`                                        | try/catch → чистый `ApiError`                                                 |
| 34       | `AbortController.signal` не прокидывался в fetch         | `useCurrentUser.ts`, `auth.api.ts`                         | signal проброшен + игнор `AbortError`                                         |
| 35       | `lobbyId!` вместо захваченного `id`                      | `useLobbyConnection.ts`                                    | используем `id`                                                               |
| 24,37    | Неочищаемые `setTimeout` (copy/saved) → leak при unmount | `LobbyRoom.tsx`, `UserPage.tsx`                            | `useRef` + cleanup-effect                                                     |

### Группа C — бэкенд-корректность

| #   | Находка                                                          | Файл                     | Фикс                                                                                |
| --- | ---------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------------- |
| 4   | Утечка `pickTimers` при завершении игры через kick/leave         | `game/game.service.ts`   | `scheduleFinishedGameCleanup` чистит таймер                                         |
| 18  | Завершённые игры навсегда в registry                             | `game/game.service.ts`   | TTL-cleanup (10 мин) → `unregisterGame`; хук в `commit()` + `endActiveGameForLobby` |
| 6   | TOCTOU: превышение `MAX_CLUBS_PER_USER` при concurrent approve   | `clubs/club.service.ts`  | ре-чек внутри Serializable-транзакции + P2034                                       |
| 20  | То же при concurrent createClub                                  | `clubs/club.service.ts`  | то же                                                                               |
| 32  | `setReady` не проверял статус лобби                              | `lobby/lobby.service.ts` | guard `status === 'WAITING'`                                                        |
| 7   | `parseCookieHeader` падал на битом URL-encoding (рвал handshake) | `plugins/socketio.ts`    | try/catch вокруг `decodeURIComponent`                                               |

### Группа D (частично) — данные

| #   | Находка                                | Файл                       | Фикс                                                                  |
| --- | -------------------------------------- | -------------------------- | --------------------------------------------------------------------- |
| 33  | Нет индекса на FK `User.primaryClubId` | `schema.prisma` + миграция | `@@index([primaryClubId])` + `20260529000000_user_primary_club_index` |

---

## ⏸ Отложено сознательно (с обоснованием)

### Split больших файлов (#3, #29, #45, #46, #16)

`game.engine.ts` 1509 · `game.service.ts` 1152 · `UserPage.tsx` 946 ·
`AdminPage.tsx` 792 · `auth.service.ts` 666. **Не бандлим в этот коммит:**
~3500 строк чисто механических перемещений с риском регрессий не должны ехать
вместе с security/correctness-фиксами (иначе коммит неревьюабелен). CLAUDE.md
сам предписывает «split when touching them» — оппортунистически, отдельными PR.
Рекомендация: по одному PR на файл, с typecheck+тестами после каждого.

### Гонки autoPick (#40, #41) — НЕ баг

`autoPickRoleCard` читает state без лока, но затем вызывает `pickRoleCard`,
который берёт `withLock` и ре-валидирует ход через `applyRoleCardPick`
(движок отклонит, если карта занята / picker сменился). Гонки нет — движок
под локом является шлюзом. Изменение только добавило бы риск.

### Engine-layer authz (#2, #17, #30) — defense-in-depth

`applyJudgeUnnominate/Foul/Unfoul/Remove` не валидируют `actor.isJudge`, но
service-слой (`requireJudge`) это гарантирует на всех путях. Не дыра;
консистентность движка — отдельная задача, трогающая ядро.

### Прочее (low / требует отдельной работы)

- #27 timing-enumeration при регистрации — требует реструктуризации register.
- #26 OAuth error логируется целиком — payload arctic неизвестен, риск низкий.
- #19/#28/#21 проглоченные ошибки / unhandled rejection в fire-and-forget — добавить логирование.
- #44/#49/#58 prisma: FK RESTRICT/enum-валидация на уровне БД.
- #42/#51/#54 unchecked-assertion / валидация server-data на клиенте.
- Тесты (#22/#23/#25/#52): Socket.IO auth-middleware, rate-limit, UserPage, AdminPage, MediaRoom — известный gap, новые тест-файлы.

### #31/#48/#56 (валидация формата path-параметров)

Реальный риск был только в debug-log (#5, файловый путь) — исправлен.
Остальные `:id`/`:code`/`:userId` идут в Prisma как TEXT-колонки (не native
uuid), поэтому невалидный формат → `findUnique` вернёт null → штатный
NOT_FOUND, а не ошибка БД. Дополнительная валидация косметична.

---

## Полные данные

Сырой JSON всех находок (описания, рекомендации, заметки обоих верификаторов,
отклонённые находки) — в выводе workflow `full-project-audit`.
